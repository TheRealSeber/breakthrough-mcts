\section{Plan eksperymentów}
\label{sec:eksperymenty}

W tej sekcji przedstawiamy konfigurację każdego z sześciu eksperymentów
odpowiadających hipotezom H1--H6.
\subsection{Wspólna konfiguracja}

Wszystkie eksperymenty mają tę samą strukturę:
\begin{itemize}[nosep]
    \item \textbf{Format turnieju:} każdy z każdym między $k$ agentami $A_1,
          \ldots, A_k$, gdzie każda uporządkowana para $(A_i, A_j)$ z $i \neq j$
          gra $n_{\text{games}}/2$ partii z $A_i$ jako białymi i $n_{\text{games}}/2$
          partii z $A_i$ jako czarnymi.
    \item \textbf{Master seed} (osobny dla każdego eksperymentu) $\to$ deterministycznie
          wyprowadzony seed na partię.
    \item \textbf{Logowanie:} format JSONL, zapisywany append-only po każdej partii. Każda linia zawiera konfiguracje obu agentów, seed, wynik,
          liczbę ruchów, czas decyzji każdego z graczy.
    \item \textbf{Wznawialność:} przy ponownym uruchomieniu skryptu, partie
          z identyfikatorem już obecnym w pliku JSONL są pomijane.
    \item \textbf{Zrównoleglenie:} \texttt{ProcessPoolExecutor} z domyślną liczbą procesów
          $= n_{\text{cores}} - 1$. \emph{MCTS wewnątrz pojedynczej partii pozostaje
          sekwencyjny.}
\end{itemize}

\subsection{H1: RAVE vs bazowy UCT}

\paragraph{Konfiguracja.}
8 agentów: \{UCT, RAVE\} $\times$ \{5000, 10\,000, 50\,000, 100\,000\} iteracji.
Stała eksploracji $c = \sqrt{2}$.
Hiperparametr RAVE: $b^2 = 0{,}01$ (dobrany empirycznie --
patrz sekcja~\ref{sec:llm}).

\paragraph{Liczba partii.} 100 partii na parę agentów (50 na każdą stronę)
$\Rightarrow$ $\binom{8}{2} = 28$ par; łącznie 2800 partii.
Master seed: 42.

\paragraph{Oczekiwania.} Zgodnie z hipotezą H1: RAVE wygrywa z UCT
przy każdym z testowanych budżetów.

\subsection{H2: Progressive Bias vs bazowy UCT}

\paragraph{Konfiguracja.}
8 agentów: \{UCT, Progressive Bias\} $\times$ \{5000, 10\,000, 50\,000, 100\,000\} iteracji.
Stała eksploracji $c = \sqrt{2}$.
Waga bias termu: $w_{\text{bias}} = 1{,}0$.

\paragraph{Liczba partii.} 100 partii na parę agentów
$\Rightarrow$ $\binom{8}{2} = 28$ par; łącznie 2800 partii. Master seed: 43.

\paragraph{Oczekiwania.} Progressive Bias wygrywa z UCT przy każdym budżecie,
z możliwie większą przewagą przy niskich budżetach.

\subsection{H3: Heurystyka vs bazowy UCT}

\paragraph{Konfiguracja.}
Agenci: heurystyka alpha-beta z głębokością 5 oraz UCT z budżetami
\{500, 1000, 5000, 10\,000, 50\,000, 100\,000\}. 100 partii na parę
$\Rightarrow$ $\binom{7}{2} = 21$ par; łącznie 2100 partii.
Master seed: 44.

\paragraph{Oczekiwania.}
Pierwsza część hipotezy: heurystyka wygrywa z UCT przy niskich budżetach
($\leq$ 5000 iteracji).
Druga część: heurystyka przegrywa z UCT przy wysokich budżetach
($\geq$ 50\,000 iteracji).

\subsection{H4: Przewaga pierwszego gracza}

\paragraph{Konfiguracja.}
Symetryczne mecze UCT(5000) vs UCT(5000) na planszy 8$\times$8 -- ten sam
agent (identyczny budżet i hiperparametry) gra jako biały i jako czarny,
przy deterministycznie różnych seedach. Master seed: 45. Łącznie 500 partii.

\paragraph{Oczekiwania.}
Białe (pierwszy gracz) mają niewielką, lecz istotną statystycznie przewagę
($>$ 50\% wygranych), analogicznie do obserwacji z szachów.

\subsection{H5: Strojenie stałej eksploracji $c$}

\paragraph{Konfiguracja.}
5 agentów UCT z budżetem 10\,000 iteracji, różniących się jedynie stałą
eksploracji $c \in \{0{,}5,\ 1{,}0,\ 1{,}5,\ 2{,}0,\ 3{,}0\}$. 100 partii na parę $\Rightarrow$ $\binom{5}{2} = 10$ par;
łącznie 1000 partii. Master seed: 46.

\paragraph{Oczekiwania.}
Istnieje optymalna wartość $c^\star$; win-rate jako funkcja $c$ ma kształt
odwróconego U (spadek przy zbyt małym i zbyt dużym $c$). Wartość teoretyczna
$c = \sqrt{2} \approx 1{,}41$ jest naturalnym kandydatem na okolice optimum.

\subsection{H6: Czas decyzji w funkcji numeru ruchu}

\paragraph{Konfiguracja.}
3 agenci UCT z budżetami \{5000, 10\,000, 50\,000\} iteracji (różne budżety,
by zbadać skalowanie czasu). Dla każdej partii rejestrowany jest czas decyzji
osobno dla każdego ruchu. 100 partii na parę $\Rightarrow$ $\binom{3}{2} = 3$
pary; łącznie 300 partii. Master seed: 47.

\paragraph{Oczekiwania.}
Przy stałym budżecie iteracji średni czas na ruch rośnie w środkowej fazie
gry (ruchy 15--40), gdy branching factor jest największy, i maleje w otwarciu
oraz końcówce.


\paragraph{Reprodukowalność.}
Wszystkie eksperymenty są w pełni reprodukowalne. Aby powtórzyć dany
eksperyment z dokładnie tymi samymi wynikami:
\begin{verbatim}
python -m breakthrough.experiments.run_h1  # H1
python -m breakthrough.experiments.run_h2  # H2
python -m breakthrough.experiments.run_h3  # H3
python -m breakthrough.experiments.run_h4  # H4
python -m breakthrough.experiments.run_h5  # H5
python -m breakthrough.experiments.run_h6  # H6
\end{verbatim}

